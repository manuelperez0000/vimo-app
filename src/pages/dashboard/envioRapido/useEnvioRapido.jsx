import { useEffect } from "react"

const useEnvioRapido = () => {

    

    /* useEffect(() => {
        getContacts()
    }, []) */
    return {
        contacts
    }
}

export default useEnvioRapido